# -----------------------------
# Core dependencies
# -----------------------------
requests>=2.31.0          # SonarQube API calls
matplotlib>=3.8.0         # Charts (pie/bar visuals) for Word reports
xlsxwriter>=3.1.2         # Excel writing, formatting and charting
python-docx>=1.1.0        # Word reports
colorama>=0.4.6           # Colorized CLI output (imported at startup)

# pandas/openpyxl were dropped in 0.3.0: reports are now written row-by-row via
# xlsxwriter's constant_memory mode and the csv module, so a large project no
# longer needs the whole result set materialised as a DataFrame.

# -----------------------------
# Packaging & Distribution
# -----------------------------
setuptools>=68.2.2
wheel>=0.41.2
twine>=4.0.2

# -----------------------------
# Development / Optional
# -----------------------------
pytest>=7.4.2             # Unit testing (optional)
black>=23.9.1             # Code formatting (optional)
